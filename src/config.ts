export const searchTermPattern = '{search}'
export const shopDir = 'shops'
export const shopFiles = [
    "swiss_wine.json",
    "books.json"
]

export const isWebExtension: boolean = !(window['browser'] === undefined)